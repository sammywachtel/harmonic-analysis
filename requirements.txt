# Production dependencies for harmonic-analysis library
# This library uses only Python standard library modules
# No external runtime dependencies required

# Note: This file exists to satisfy requirements-dev.txt which includes "-r requirements.txt"
# but the library itself has no external runtime dependencies.
